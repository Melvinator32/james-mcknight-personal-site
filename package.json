{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node scripts/preinstall.mjs",
    "build": "node scripts/build-site.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:workspace": "pnpm run typecheck && pnpm -r --if-present run build",
    "dev": "PORT=4173 BASE_PATH=/ node artifacts/james-mcknight-portfolio/node_modules/vite/bin/vite.js --config artifacts/james-mcknight-portfolio/vite.config.ts"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  },
  "displayName": "James McKnight Portfolio"
}
